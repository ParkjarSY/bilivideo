<view class="main">
	<!-- 导航 -->
   <Headers></Headers>
   <!-- 首页导航 -->
   <view class="nav_daoh">
	   <!-- 滚动组件 -->
	   <scroll-view class="nav" scroll-x>
		   <view class="nav-item" wx:for="{{navList}}"  wx:key="{{index}}">{{item.text}}</view>
	   </scroll-view>
   </view>
</view>